name: autobasketballbot

on:
  workflow_dispatch:
  schedule:
    - cron: '0 12 * * *'

jobs:
  run-autobasketballbot:
    runs-on: macOS-latest
    env:
      OWM_API_KEY: ${{ secrets.OWM_API_KEY }}
      AQI_API_KEY: ${{ secrets.AQI_API_KEY }}
      RTOOT_DEFAULT_TOKEN_BASKETBALL: ${{ secrets.RTOOT_DEFAULT_TOKEN_BASKETBALL }}
      GITHUB_PAT: ${{ secrets.GITHUB_TOKEN }}
    steps:
      - uses: actions/checkout@v2
      
      - uses: r-lib/actions/setup-r@v2
          
      - name: Restore R package cache
        id: cache-r-packages
        uses: actions/cache@v2
        with:
          path: ${{ env.R_LIBS_USER }}
          key: ${{ runner.os }}-${{ hashFiles('install-packages.R') }}-1
          
      - name: Install packages
        if: steps.cache-r-packages.outputs.cache-hit != 'true'        
        run: R -e 'source("install-packages.R", echo = TRUE)'
        
      - name: Download Parse and Update Chart Data
        run: Rscript basketballbot.R
        
      # Add new files in folder, commit along with other modified files, push
      - name: Commit and push if it changed
        run: |-
          git config user.name "autobasketballbot"
          git config user.email "actions@users.noreply.github.com"
          git add -A
          timestamp=$(date -u)
          git commit -m "Latest basketball data: ${timestamp}" || exit 0
          git push
